from setuptools import setup, find_packages


setup(
    name="emergentintegrations",
    version="0.1.0",
    packages=find_packages(where="."),
    package_dir={"": "."},
    description="Local stub for the missing emergentintegrations package.",
    author="Document Chatbot Team",
)
